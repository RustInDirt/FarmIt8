# FarmIt8
An expanded version of FarmIt with 8 item tracking slots

FarmIt8 is an enhanced fork of the fixed FarmIt addon, adding 8 total tracking slots while maintaining full compatibility with Vanilla WoW 1.12.1.


✨ Additional Features

8 total item tracking slots (double the original)

Slots stack vertically with updated UI layout

Fully extended background panel to wrap all 8 slots

Same simple drag-and-drop behavior:

Left-click (with cursor item) → assign item

Right-click → clear slot

Frame can still be moved freely

🛠 Fixes & Enhancements

Includes all fixes from the restored FarmIt version, plus:

Extended SavedVariables structure to support slots 5–8

Updated XML layout with correct anchors and frame offsets

Updated addon frame size to properly contain the 8 slots

Updated TOC and filenames

Cleaned Lua function replacement for maximum Vanilla compatibility

Ensures right-click removal works on all slots

*Fixed using ChatGPT
